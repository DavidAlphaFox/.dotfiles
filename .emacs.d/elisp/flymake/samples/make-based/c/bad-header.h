typedef struct _tag_A
{
  double a[-1];
} A;
/* #### (setq flymake-acc-ler '("bad-master.c" 4 "w" "warning: return makes integer from pointer without a cast" "bad-master.c"))
   #### (setq flymake-acc-ler '(nil 3 "e" "size of array `a' is negative" "bad-header.h"))
*/
